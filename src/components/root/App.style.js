import { makeStyles } from "@material-ui/core/styles";

export const appStyle = makeStyles((theme) => ({
  root: {
    // backgroundColor: "#8C1CF1",
    backgroundImage: "linear-gradient(#7f24ca, #863ec6)"
  },
  parentDiv: {
    paddingTop: "8%",
  }
}));
